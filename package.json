{
  "name": "ai-therapy-app",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.7",
    "postcss": "^8.4.14",
    "tailwindcss": "^3.1.6",
    "vite": "^4.0.0"
  }
}
git commit -m "Add Next.js to dependencies"
git add package.json
git commit -m "Add Next.js framework for Vercel"
git push origin main
git add .
git commit -m "Add VitalMind Health index page and deployment scaffold"
git push origin main

